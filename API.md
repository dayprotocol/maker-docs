# Maker API reference\n\nBase REST: `https://dayprotocol.com/mm/v1`  \nMCP: `https://dayprotocol.com/mm/mcp`  \nGenerated from live `/v1/capabilities`.\n\n## Wallet modes

| Mode | Keys | Tools |
|--|--|--|
| **External** | You hold / you sign | `POST /wallets/external`, `/trades/build`, `/trades/:id/submit` · MCP `maker_external_wallet_*`, `maker_trade_*` |
| **Custodial** | Maker vault | wallets batch, funding allocate, warmup, strategies, withdraw |

Full write-up: [WALLET-MODES.md](./WALLET-MODES.md).

## Auth\n\n```http\nAuthorization: Bearer mk_live_…\n# or\nx-api-key: mk_live_…\n```\n\nPublic (no auth): health, capabilities, strategy-templates, token lookup/intel, strategy recommend/campaign.\n\n## Parameter conventions\n\n```json\n{ "min": 1, "max": 10, "unit": "sol" }\n```\n\n- Time units: `minutes` | `hours` | `days`\n- Amount units: `sol` | `token` | `usdc` | `pct` | `bps`\n- Pin `min=max` for fixed values\n\n## REST endpoints\n\n| Method | Path | Auth | Summary |\n|--------|------|------|---------|\n| GET | `/v1/health` | yes | Liveness + privacy/x402/swap/market flags + counts |\n| GET | `/v1/capabilities` | yes | Master catalog: what_you_can_do, MCP tools (full schemas), REST endpoints, defau |\n| GET | `/v1/x402/pricing` | yes | Payment requirements: USDC 0.01 or SOL 0.0001 per success |\n| GET | `/v1/strategy-templates` | yes | Strategy templates + lifecycle campaigns (standard/fast/stealth_pace) |\n| GET | `/v1/markets/phoenix` | yes | Known Phoenix market registry entries |\n| POST | `/v1/api-keys` | yes | Create API key (returned once; store it) |\n| GET | `/v1/api-keys` | yes | List API key prefixes (no secrets) |\n| DELETE | `/v1/api-keys/:id` | yes | Revoke API key by id |\n| POST | `/v1/wallets/batch` | yes | Create N custodial wallets (paced; keys never exported) |\n| GET | `/v1/wallets` | yes | List wallets (id, pubkey, status). Optional status filter |\n| GET | `/v1/wallets/:id` | yes | Get one wallet (public fields only) |\n| POST | `/v1/wallets/withdraw` | yes | Withdraw SOL from worker to user address (no key export) |\n| POST | `/v1/wallets/external` | yes | Register a USER-OWNED wallet by pubkey — non-custodial, we never hold the privat |\n| POST | `/v1/trades/build` | yes | Build UNSIGNED swap for an external wallet — owner signs client-side (~60s block |\n| POST | `/v1/trades/:id/submit` | yes | Submit owner-signed tx — verified against the exact built message (hash + ed2551 |\n| GET | `/v1/trades` | yes | List non-custodial trades (status filter, limit) |\n| GET | `/v1/trades/:id` | yes | Get one non-custodial trade (status, signature, quote) |\n| POST | `/v1/funding/sessions` | yes | SOL-only deposit address for this org/session |\n| GET | `/v1/funding/sessions/:id` | yes | Deposit session status + org available_sol |\n| GET | `/v1/funding/balance` | yes | Org available SOL credit from deposits |\n| POST | `/v1/funding/allocate` | yes | Fund wallets via private funding only (paced) |\n| POST | `/v1/wallets/warmup` | yes | Season wallets. Fully customizable: tokens/contracts list and trading ranges. Om |\n| POST | `/v1/strategies` | yes | Create strategy; token_mint + wallet_ids required |\n| GET | `/v1/strategies` | yes | List all strategies for the org |\n| GET | `/v1/strategies/:id` | yes | Strategy status: params, inventory, budgets, campaign end |\n| POST | `/v1/strategies/:id/start` | yes | Start or resume strategy |\n| POST | `/v1/strategies/:id/pause` | yes | Pause strategy (no new risk; inventory held) |\n| POST | `/v1/strategies/:id/stop` | yes | Stop strategy |\n| PATCH | `/v1/strategies/:id` | yes | Override any strategy ranges live (not token_mint) |\n| POST | `/v1/strategies/:id/commands/buy` | yes | Discretionary buy: amount + execution_duration ranges required |\n| POST | `/v1/strategies/:id/commands/sell` | yes | Discretionary sell: amount + execution_duration ranges required |\n| POST | `/v1/strategies/:id/flatten` | yes | Reduce inventory with amount + execution_duration ranges |\n| GET | `/v1/roi` | yes | Results & ROI across jobs/strategies: realized_pnl_sol, inventory, volume, by_te |\n| GET | `/v1/jobs/:id` | yes | Unified job heartbeat (create/fund/warmup/strategy/command) |\n| POST | `/v1/jobs/:id/cancel` | yes | Cancel job; warmup sells ASAP; wallets kept |\n| PATCH | `/v1/jobs/:id` | yes | Live-update create/fund/warmup params (tokens, ranges) |\n| POST | `/v1/campaigns` | yes | One-shot lifecycle: paced wallets → private fund → warmup → optional auto strate |\n| GET|POST | `/mcp` | yes | Hosted MCP Streamable HTTP. GET returns full tool catalog; POST is JSON-RPC (ini |\n\n## Endpoint details\n\n### GET `/v1/health`\n\nLiveness + privacy/x402/swap/market flags + counts\n\n```bash\ncurl -s -X GET "https://dayprotocol.com/mm/v1/v1/health"\n```\n\n### GET `/v1/capabilities`\n\nMaster catalog: what_you_can_do, MCP tools (full schemas), REST endpoints, defaults, x402, rules\n\n```bash\ncurl -s -X GET "https://dayprotocol.com/mm/v1/v1/capabilities"\n```\n\n### GET `/v1/x402/pricing`\n\nPayment requirements: USDC 0.01 or SOL 0.0001 per success\n\n```bash\ncurl -s -X GET "https://dayprotocol.com/mm/v1/v1/x402/pricing"\n```\n\n### GET `/v1/strategy-templates`\n\nStrategy templates + lifecycle campaigns (standard/fast/stealth_pace)\n\n```bash\ncurl -s -X GET "https://dayprotocol.com/mm/v1/v1/strategy-templates"\n```\n\n### GET `/v1/markets/phoenix`\n\nKnown Phoenix market registry entries\n\n```bash\ncurl -s -X GET "https://dayprotocol.com/mm/v1/v1/markets/phoenix" -H "authorization: Bearer $KEY"\n```\n\n### POST `/v1/api-keys`\n\nCreate API key (returned once; store it)\n\n```bash\ncurl -s -X POST "https://dayprotocol.com/mm/v1/v1/api-keys" -H "authorization: Bearer $KEY" -H "content-type: application/json" -d '{...}'\n```\n\n**Inputs:**\n- `label` (string, optional) \n\n### GET `/v1/api-keys`\n\nList API key prefixes (no secrets)\n\n```bash\ncurl -s -X GET "https://dayprotocol.com/mm/v1/v1/api-keys" -H "authorization: Bearer $KEY"\n```\n\n### DELETE `/v1/api-keys/:id`\n\nRevoke API key by id\n\n```bash\ncurl -s -X DELETE "https://dayprotocol.com/mm/v1/v1/api-keys/<id>" -H "authorization: Bearer $KEY"\n```\n\n**Inputs:**\n- `id` (string, required) \n\n### POST `/v1/wallets/batch`\n\nCreate N custodial wallets (paced; keys never exported)\n\n```bash\ncurl -s -X POST "https://dayprotocol.com/mm/v1/v1/wallets/batch" -H "authorization: Bearer $KEY" -H "content-type: application/json" -d '{...}'\n```\n\n**Inputs:**\n- `count` (integer, required) \n- `create_per_day` (range, optional) \n- `create_spacing_minutes` (range, optional) \n- `label_prefix` (string, optional) \n- `purpose` (string, optional) \n\n### GET `/v1/wallets`\n\nList wallets (id, pubkey, status). Optional status filter\n\n```bash\ncurl -s -X GET "https://dayprotocol.com/mm/v1/v1/wallets" -H "authorization: Bearer $KEY"\n```\n\n**Inputs:**\n- `status` (string, optional) created|funded|warming|ready|disabled\n\n### GET `/v1/wallets/:id`\n\nGet one wallet (public fields only)\n\n```bash\ncurl -s -X GET "https://dayprotocol.com/mm/v1/v1/wallets/<id>" -H "authorization: Bearer $KEY"\n```\n\n**Inputs:**\n- `id` (string, required) \n\n### POST `/v1/wallets/withdraw`\n\nWithdraw SOL from worker to user address (no key export)\n\n```bash\ncurl -s -X POST "https://dayprotocol.com/mm/v1/v1/wallets/withdraw" -H "authorization: Bearer $KEY" -H "content-type: application/json" -d '{...}'\n```\n\n**Inputs:**\n- `wallet_id` (string, required) \n- `to_address` (string, required) \n- `amount` (amount_range, required) \n- `execution_duration` (time_range, optional) \n\n### POST `/v1/wallets/external`\n\nRegister a USER-OWNED wallet by pubkey — non-custodial, we never hold the private key\n\n```bash\ncurl -s -X POST "https://dayprotocol.com/mm/v1/v1/wallets/external" -H "authorization: Bearer $KEY" -H "content-type: application/json" -d '{...}'\n```\n\n**Inputs:**\n- `pubkey` (string, required) \n- `label` (string, optional) \n\n### POST `/v1/trades/build`\n\nBuild UNSIGNED swap for an external wallet — owner signs client-side (~60s blockhash window)\n\n```bash\ncurl -s -X POST "https://dayprotocol.com/mm/v1/v1/trades/build" -H "authorization: Bearer $KEY" -H "content-type: application/json" -d '{...}'\n```\n\n**Inputs:**\n- `wallet_id` (string, optional) \n- `pubkey` (string, optional) \n- `input_mint` (string, required) \n- `output_mint` (string, required) \n- `amount_lamports` (number, optional) \n- `amount_sol` (number, optional) \n- `slippage_bps` (number, optional) \n\n### POST `/v1/trades/:id/submit`\n\nSubmit owner-signed tx — verified against the exact built message (hash + ed25519) before relay\n\n```bash\ncurl -s -X POST "https://dayprotocol.com/mm/v1/v1/trades/<id>/submit" -H "authorization: Bearer $KEY" -H "content-type: application/json" -d '{...}'\n```\n\n**Inputs:**\n- `signed_tx_base64` (string, required) \n\n### GET `/v1/trades`\n\nList non-custodial trades (status filter, limit)\n\n```bash\ncurl -s -X GET "https://dayprotocol.com/mm/v1/v1/trades" -H "authorization: Bearer $KEY"\n```\n\n**Inputs:**\n- `status` (string, optional) \n- `limit` (number, optional) \n\n### GET `/v1/trades/:id`\n\nGet one non-custodial trade (status, signature, quote)\n\n```bash\ncurl -s -X GET "https://dayprotocol.com/mm/v1/v1/trades/<id>" -H "authorization: Bearer $KEY"\n```\n\n### POST `/v1/funding/sessions`\n\nSOL-only deposit address for this org/session\n\n```bash\ncurl -s -X POST "https://dayprotocol.com/mm/v1/v1/funding/sessions" -H "authorization: Bearer $KEY" -H "content-type: application/json" -d '{...}'\n```\n\n### GET `/v1/funding/sessions/:id`\n\nDeposit session status + org available_sol\n\n```bash\ncurl -s -X GET "https://dayprotocol.com/mm/v1/v1/funding/sessions/<id>" -H "authorization: Bearer $KEY"\n```\n\n**Inputs:**\n- `id` (string, required) \n\n### GET `/v1/funding/balance`\n\nOrg available SOL credit from deposits\n\n```bash\ncurl -s -X GET "https://dayprotocol.com/mm/v1/v1/funding/balance" -H "authorization: Bearer $KEY"\n```\n\n### POST `/v1/funding/allocate`\n\nFund wallets via private funding only (paced)\n\n```bash\ncurl -s -X POST "https://dayprotocol.com/mm/v1/v1/funding/allocate" -H "authorization: Bearer $KEY" -H "content-type: application/json" -d '{...}'\n```\n\n**Inputs:**\n- `wallet_ids` (string[]|all_unfunded, required) \n- `amount_range_sol` (range, optional) \n- `amounts_sol` (number[], optional) Optional 1:1 with wallet_ids\n- `fund_per_day` (range, optional) \n- `fund_spacing_minutes` (range, optional) \n- `session_id` (string, optional) \n\n### POST `/v1/wallets/warmup`\n\nSeason wallets. Fully customizable: tokens/contracts list and trading ranges. Omit tokens → high-volume discovery.\n\n```bash\ncurl -s -X POST "https://dayprotocol.com/mm/v1/v1/wallets/warmup" -H "authorization: Bearer $KEY" -H "content-type: application/json" -d '{...}'\n```\n\n**Inputs:**\n- `wallet_ids` (string[]|all_funded, required) \n- `tokens` (array, optional) Optional SPL mints: string[] or {mint, weight?, quote_mint?}[]. Aliases: token_mints, contracts\n- `use_discovery` (boolean, optional) \n- `days` (time_range, optional) \n- `activities_per_day` (range, optional) \n- `hold_hours` (time_range, optional) \n- `sell_pct` (range, optional) \n- `notional_sol` (range, optional) \n- `notional_pct_of_wallet` (range, optional) \n- `notional_cap_sol` (range, optional) \n- `slippage_bps` (range, optional) \n- `active_hours_utc` (range, optional) \n- `class_weights` (object, optional) bluechip / high_volume_token / nft (nft default 0)\n- `quote_mint` (string, optional) \n\n### POST `/v1/strategies`\n\nCreate strategy; token_mint + wallet_ids required\n\n```bash\ncurl -s -X POST "https://dayprotocol.com/mm/v1/v1/strategies" -H "authorization: Bearer $KEY" -H "content-type: application/json" -d '{...}'\n```\n\n**Inputs:**\n- `token_mint` (string, required) \n- `wallet_ids` (string[], required) \n- `quote_mint` (string, optional) \n- `template` (string, optional) \n- `overrides` (partial StrategyParams ranges, optional) \n- `campaign_duration` (time_range, optional) \n- `max_notional_per_hour_sol` (range, optional) \n- `notional_per_trade_sol` (range, optional) \n- `take_profit_pct` (range, optional) \n- `dip_trigger_pct` (range, optional) \n- `organic_volume_zscore` (range, optional) \n\n### GET `/v1/strategies`\n\nList all strategies for the org\n\n```bash\ncurl -s -X GET "https://dayprotocol.com/mm/v1/v1/strategies" -H "authorization: Bearer $KEY"\n```\n\n### GET `/v1/strategies/:id`\n\nStrategy status: params, inventory, budgets, campaign end\n\n```bash\ncurl -s -X GET "https://dayprotocol.com/mm/v1/v1/strategies/<id>" -H "authorization: Bearer $KEY"\n```\n\n**Inputs:**\n- `id` (string, required) \n\n### POST `/v1/strategies/:id/start`\n\nStart or resume strategy\n\n```bash\ncurl -s -X POST "https://dayprotocol.com/mm/v1/v1/strategies/<id>/start" -H "authorization: Bearer $KEY" -H "content-type: application/json" -d '{...}'\n```\n\n### POST `/v1/strategies/:id/pause`\n\nPause strategy (no new risk; inventory held)\n\n```bash\ncurl -s -X POST "https://dayprotocol.com/mm/v1/v1/strategies/<id>/pause" -H "authorization: Bearer $KEY" -H "content-type: application/json" -d '{...}'\n```\n\n### POST `/v1/strategies/:id/stop`\n\nStop strategy\n\n```bash\ncurl -s -X POST "https://dayprotocol.com/mm/v1/v1/strategies/<id>/stop" -H "authorization: Bearer $KEY" -H "content-type: application/json" -d '{...}'\n```\n\n### PATCH `/v1/strategies/:id`\n\nOverride any strategy ranges live (not token_mint)\n\n```bash\ncurl -s -X PATCH "https://dayprotocol.com/mm/v1/v1/strategies/<id>" -H "authorization: Bearer $KEY" -H "content-type: application/json" -d '{...}'\n```\n\n**Inputs:**\n- `overrides` (partial StrategyParams ranges, optional) \n\n### POST `/v1/strategies/:id/commands/buy`\n\nDiscretionary buy: amount + execution_duration ranges required\n\n```bash\ncurl -s -X POST "https://dayprotocol.com/mm/v1/v1/strategies/<id>/commands/buy" -H "authorization: Bearer $KEY" -H "content-type: application/json" -d '{...}'\n```\n\n**Inputs:**\n- `amount` (amount_range, required) \n- `execution_duration` (time_range, required) \n\n### POST `/v1/strategies/:id/commands/sell`\n\nDiscretionary sell: amount + execution_duration ranges required\n\n```bash\ncurl -s -X POST "https://dayprotocol.com/mm/v1/v1/strategies/<id>/commands/sell" -H "authorization: Bearer $KEY" -H "content-type: application/json" -d '{...}'\n```\n\n**Inputs:**\n- `amount` (amount_range, required) \n- `execution_duration` (time_range, required) \n- `sell_pct` (range, optional) \n\n### POST `/v1/strategies/:id/flatten`\n\nReduce inventory with amount + execution_duration ranges\n\n```bash\ncurl -s -X POST "https://dayprotocol.com/mm/v1/v1/strategies/<id>/flatten" -H "authorization: Bearer $KEY" -H "content-type: application/json" -d '{...}'\n```\n\n**Inputs:**\n- `amount` (amount_range, optional) \n- `execution_duration` (time_range, optional) \n\n### GET `/v1/roi`\n\nResults & ROI across jobs/strategies: realized_pnl_sol, inventory, volume, by_template comparison, per-job outcomes. Filters: strategy_id, job_id, template, kind, since, limit\n\n```bash\ncurl -s -X GET "https://dayprotocol.com/mm/v1/v1/roi"\n```\n\n**Inputs:**\n- `strategy_id` (string, optional) \n- `job_id` (string, optional) \n- `template` (string, optional) \n- `kind` (string, optional) \n- `since` (string, optional) \n- `limit` (number, optional) \n\n### GET `/v1/jobs/:id`\n\nUnified job heartbeat (create/fund/warmup/strategy/command)\n\n```bash\ncurl -s -X GET "https://dayprotocol.com/mm/v1/v1/jobs/<id>" -H "authorization: Bearer $KEY"\n```\n\n### POST `/v1/jobs/:id/cancel`\n\nCancel job; warmup sells ASAP; wallets kept\n\n```bash\ncurl -s -X POST "https://dayprotocol.com/mm/v1/v1/jobs/<id>/cancel" -H "authorization: Bearer $KEY" -H "content-type: application/json" -d '{...}'\n```\n\n### PATCH `/v1/jobs/:id`\n\nLive-update create/fund/warmup params (tokens, ranges)\n\n```bash\ncurl -s -X PATCH "https://dayprotocol.com/mm/v1/v1/jobs/<id>" -H "authorization: Bearer $KEY" -H "content-type: application/json" -d '{...}'\n```\n\n**Inputs:**\n- `tokens` (array, optional) \n- `contracts` (string[], optional) \n- `notional_sol` (range, optional) \n- `hold_hours` (time_range, optional) \n- `sell_pct` (range, optional) \n- `days` (time_range, optional) \n- `activities_per_day` (range, optional) \n- `slippage_bps` (range, optional) \n\n### POST `/v1/campaigns`\n\nOne-shot lifecycle: paced wallets → private fund → warmup → optional auto strategy\n\n```bash\ncurl -s -X POST "https://dayprotocol.com/mm/v1/v1/campaigns" -H "authorization: Bearer $KEY" -H "content-type: application/json" -d '{...}'\n```\n\n**Inputs:**\n- `wallet_count` (integer, required) \n- `template` (string, optional) lifecycle_standard|lifecycle_fast|lifecycle_stealth_pace\n- `token_mint` (string, optional) Contract for strategy after warmup\n\n### GET|POST `/mcp`\n\nHosted MCP Streamable HTTP. GET returns full tool catalog; POST is JSON-RPC (initialize, tools/list, tools/call, resources/*)\n\n```bash\ncurl -s -X GET "https://dayprotocol.com/mm/v1/mcp" -H "content-type: application/json" -d '{...}'\n```\n\n## MCP tools\n\nConnect to `https://dayprotocol.com/mm/mcp`. Pass `api_key` on org-scoped tools.\n\n| Tool | Description |\n|------|-------------|\n| `maker_capabilities` | LIST ALL API CALLS: returns every REST endpoint + MCP tool with instructions, curl, inputs, auth. Also how_to_test (1=cr |\n| `maker_health` | Liveness: funding rail mode, x402 on/off, swap/market flags, wallet/job counts. Free. |\n| `maker_x402_pricing` | Pricing status. Currently free (X402 off): usdc_per_call=0, sol_per_call=0. Re-enable later via X402_ENABLED=true. |\n| `maker_list_templates` | FULL STRATEGY GUIDE: all templates (PMM, volume_boost/bump/advanced multi-maker, dip/TP, LP scale), venues (Meteora DLMM |\n| `maker_phoenix_markets` | List known Phoenix market registry entries (base/quote mints). Configure via phoenix-markets.json. |\n| `maker_token_lookup` | Look up a Solana token by CONTRACT/mint address via market data. Returns symbol, name, price_usd, liquidity, volume (m5/ |\n| `maker_lp_scale_plan` | Build a one-sided or two-sided LP scale-in/out ladder plan for a mint. Venues: Meteora DLMM (default), Raydium CLMM, Orc |\n| `maker_token_intel` | FULL token forensics for decision-making: market tape (liq, vol m5/h1/h6/h24, txns, price Δ, pair age, trades/day est),  |\n| `maker_strategy_campaign` | BEST DECISION PATH: full intel (market+holders+traders) → score ALL strategy templates → ranked scorecard → execute_orde |\n| `maker_strategy_recommend` | Quick regime recommend (market activity only). For best decisions prefer maker_strategy_campaign (intel + full scorecard |\n| `maker_plan` | ORCHESTRATOR: turn freeform intent into a multi-step job plan (research / BYO / create+fund / warmup / strategy). Fills  |\n| `maker_execute_plan` | ORCHESTRATOR: execute a plan from maker_plan after user confirmation. Requires plan_id + (confirm_token matching the pla |\n| `maker_create_api_key` | Mint long-lived API key = your Maker account. CRITICAL: response.key (mk_live_…) is shown ONCE — save it immediately. Se |\n| `maker_account` | Look up your account from API key: org_id, wallet/job/strategy counts, available_sol + open deposit_addresses. API is fr |\n| `maker_list_api_keys` | List API key METADATA for YOUR org only: id, short prefix (mk_live_xxxx), label, created_at. NEVER returns full secrets  |\n| `maker_revoke_api_key` | Revoke an API key by id. |\n| `maker_wallets_batch` | CREATE custodial worker wallets. Private keys are generated server-side, encrypted in vault, and NEVER returned/exported |\n| `maker_wallets_list` | List wallets (id, pubkey, status: created/funded/warming/ready/disabled). Optional status filter. |\n| `maker_wallet_get` | Get one wallet by id (public fields only — no private key). |\n| `maker_wallet_withdraw` | PULL FUNDS OUT of a custodial worker into the user's own wallet. ONE-SHOT SOL transfer to to_address. Maker keeps operat |\n| `maker_external_wallet_register` | Register a USER-OWNED wallet by pubkey only. NON-CUSTODIAL: Maker NEVER receives, stores, or logs the private key — pubk |\n| `maker_trade_build` | Build an UNSIGNED swap for a registered external wallet. Returns unsigned_tx_base64 + trade_id. OWNER signs client-side  |\n| `maker_trade_submit` | Relay an owner-signed tx for a built trade. Verifies sha256 message-hash matches what we built + owner ed25519 signature |\n| `maker_trades_list` | List non-custodial trades (built/confirmed/failed/expired). |\n| `maker_trade_get` | Get one non-custodial trade by id (status, signature, quote). |\n| `maker_funding_session` | Create SOL deposit address for this org/session. User sends SOL here; watcher credits available_sol. Always SOL-only int |\n| `maker_funding_session_get` | Get deposit session status + org available_sol. |\n| `maker_funding_balance` | Org available SOL credit (from deposits). |\n| `maker_funding_allocate` | FUND workers via private rail (never public hop). DEFAULT = deposit ONCE to session, then STAGGERED drip to many wallets |\n| `maker_warmup` | WARMUP: season wallets with buy/hold/sell. FULLY CUSTOMIZABLE. Optional tokens/contracts list (weighted mints); if omitt |\n| `maker_strategy_create` | Create MM strategy on a token. REQUIRED: token_mint + wallet_ids (custodial workers). Optional template (default balance |\n| `maker_strategy_list` | List all strategies for the org. |\n| `maker_strategy_get` | Get strategy status: params, inventory, hourly budget used, organic flow signals, campaign_ends_at. |\n| `maker_strategy_start` | Start or resume a strategy (begins quoting / dip / flow modes per template). |\n| `maker_strategy_pause` | Pause strategy (no new risk; inventory held). |\n| `maker_strategy_stop` | Stop strategy (campaign end path; settle separately via flatten if needed). |\n| `maker_strategy_patch` | LIVE override any strategy ranges while running (volume/hour, spreads, TP, dip, organic, campaign duration, slippage, ji |\n| `maker_strategy_flatten` | Reduce inventory. amount range (default 90-100% inventory) + execution_duration range (paced exit). |\n| `maker_command_buy` | Discretionary BUY on a strategy (POST /v1/strategies/:id/commands/buy). REQUIRED ranges: amount {min,max,unit:sol} and e |\n| `maker_command_sell` | Discretionary SELL on a strategy (POST /v1/strategies/:id/commands/sell). REQUIRED ranges: amount + execution_duration.  |\n| `maker_roi` | RESULTS & ROI across jobs and strategies you started. Returns summary (total realized_pnl_sol, open inventory, hourly/da |\n| `maker_jobs_list` | List all jobs for your account (status/kind/q filters). Every MM action creates a job with human name, progress, activit |\n| `maker_job_status` | Job detail: name, status, progress {pct,done,total,message}, activity[] trail, next_at, open_positions, complete_when. |\n| `maker_job_cancel` | Cancel job; warmup legs sell ASAP. Does not delete wallets. |\n| `maker_job_patch` | LIVE-EDIT a running/scheduled job after create. Change randomness, speed up, retune amounts, rename. fund_wallets: amoun |\n| `maker_campaign` | ONE-SHOT LIFECYCLE: create paced wallets → private fund → warmup → auto-start strategy. Returns deposit_address (send SO |\n